je suis sur un projet jsp & java, j utilise une database dans oracle (vaniala/vaniala) . Je suis senser gerer la gestion des medicaments dans 
une pharmacie, dans le fichier AS_INGREDIENTS_corriger.txt se trouve la liste des medicaments, et voici la liste des unites:
create table AS_UNITE_2(
    id VARCHAR2(20),
    val VARCHAR2(20),
    desce VARCHAR2(20)
);
INSERT INTO AS_UNITE_2 (id,val,desce) VALUES
("UNIT0002","plaquette","plaquette"),
("STU000007152420","unite","unite"),
("UNI000006","boite","boite"),
("UNT001106","carton","carton"),
("UNT00005","unite","unite") ;

j ai une table 'equivalence' :
-- 1) Création de la table avec clé primaire
CREATE TABLE equivalence (
    id          VARCHAR2(20) PRIMARY KEY,
    idproduit   VARCHAR2(20),
    unite       VARCHAR2(20),
    unite_ref   VARCHAR2(20),
    quantite    NUMBER(10,2),
    pv          NUMBER(10,2)
);

-- 2) Séquence pour générer les ID
CREATE SEQUENCE seq_equivalence
    START WITH 1
    INCREMENT BY 1
    NOCACHE
    NOCYCLE;
/
-- 3) Trigger BEFORE INSERT
CREATE OR REPLACE TRIGGER equivalence_before_insert
BEFORE INSERT ON equivalence
FOR EACH ROW
BEGIN
    :NEW.id := 'EQUI00-' || seq_equivalence.NEXTVAL;
END;
/

je veux maintenant que tu me genere les donnes a inserer dans la table 'equivalence'.
je veux une equivalence des unite, c est a dire que la table 'equivalence' soit un repere d equivalence
pour les unite.
pour chaque medicaments (as_ingredients), tu complet dans la table 'equivalence' ceci:
  - idproduit = id (dans as_ingredients)
  - unite = le unite a trouver la convertion
  - unite_ref = unite (dans as_ingredients) --- c'est unite plus petite de ce produit
  - quantite = la quantite de piece(unite_ref) contenu dans unite  ---- c'est la quantite qui fait l'equivalence
  - pv = pv(dans as_ingredients) * quantite(dans equivalence) - reduction (5%)   ----- le prix de vente fixe de ce produit avec cet unite(equivalence) 

Voici les equivalences que je veux appliquer:
  - plaquette = 10 unite
  - boite = 20 unite
  - carton = 30 unite
